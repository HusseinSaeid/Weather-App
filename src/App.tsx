import WeatherCard from "./WeatherCard";

export default function App() {
  return (
    <div className="App h-screen w-screen flex items-center justify-center bg-[#fff]">
      <WeatherCard />
    </div>
  );
}
